1. S-Power 
- Power Generater Output predict (by Machine Learning) 

2. ES OCR A/S Report, SCL Boiler Insurance 
- PDF -> PNG -> Naver Clova OCR -> Text 
- 동일한 양식, 한글 필기체(손글씨) 포함 -> Naver Clova OCR(templete) 사용 
- NCP API key, Invoke URL, OCR Domain, OCR templete builing 필요 
- 참고 사이트 : https://davelogs.tistory.com/40 

3. ES Contract AI Check 
[ 계약서 리스크 검토 프로그램 사용자 가이드 ]

이 프로그램은 계약서 문서를 자동으로 분석하여 리스크 항목을 추출하고,
엑셀 파일로 결과를 저장해주는 도구입니다.
아래 순서대로 따라주세요.

------------------------------------------------------------
1. 계약서 문서 준비
------------------------------------------------------------
- 바탕화면 또는 원하는 위치에 'contract' 폴더를 생성합니다.
- 검토하려는 계약서 파일을 해당 폴더에 저장합니다.
  (지원 파일: PDF 또는 Word 문서 / 확장자: .pdf, .docx)

------------------------------------------------------------
2. API 키 설정 (개발자가 실행)
------------------------------------------------------------
- 아래와 같은 형식의 OpenAI API Key를 발급받아
- 개발자에게 전달하여 프로그램의 수정을 요청합니다. 

    API_KEY = "sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

- API_KEY 수정적용 후 .py --> .exe를 위한 rebuild 필요 
: pyinstaller --onefile contract_check_v1.0.py
------------------------------------------------------------
3. 프로그램 실행
------------------------------------------------------------
- dist 폴더 안에 있는 contract_check_v1.0.exe 파일을 더블클릭합니다.
- 프로그램이 실행되면 문서 선택 창이 나타납니다.

------------------------------------------------------------
4. 계약서, 프롬프트 문서 선택
------------------------------------------------------------
- contract 폴더에 저장한 계약서 문서를 선택합니다.
- prompt 폴더에 저장한 prompt.txt 파일을 선택합니다. 
- 선택 후 자동으로 분석이 시작됩니다.
- 분석이 완료되면 output 폴더에 결과 엑셀 파일이 생성됩니다.

------------------------------------------------------------
5. 결과 확인
------------------------------------------------------------
- output 폴더를 열어 .xlsx 파일을 확인합니다.
- 엑셀 파일에는 다음과 같은 항목이 포함됩니다:

    항목 | Page | 본문 | Risk | 위험요인 | 개선사항

------------------------------------------------------------
기타 안내
------------------------------------------------------------
- 프로그램은 인터넷 연결이 필요합니다 (OpenAI API 사용).
- 문서가 길거나 복잡한 경우 분석 시간이 다소 걸릴 수 있습니다.
- 결과 파일은 자동으로 시간별로 저장되므로 덮어쓰지 않습니다.
------------------------------------------------------------
